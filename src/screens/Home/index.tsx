import React from 'react';
import {Text} from 'react-native';
import CFlex from '@/components/layout/CFlex.tsx';

const Home = () => {
  return (
    <CFlex>
      <Text>Home</Text>
    </CFlex>
  );
};

export default Home;
